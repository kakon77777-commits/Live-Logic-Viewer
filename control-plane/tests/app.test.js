import { describe, expect, it } from 'vitest'
import { createApplication } from '../src/app.js'
import { createMockProvider } from '../src/providers/mock.js'
const env={CONTROL_API_TOKEN:'0123456789abcdef0123456789abcdef',VIEWER_ORIGIN:'https://viewer.example',ALLOW_UNLIMITED_DEV:'true'}
const requestBody={schema_version:'0.1',runner:'python',source:'print(1 + 1)',network_policy:{mode:'deny'},limits:{wall_ms:5000,output_bytes:65536}}
function jobRequest(extra={}){return new Request('https://control.example/v1/jobs',{method:'POST',headers:{authorization:`Bearer ${env.CONTROL_API_TOKEN}`,'content-type':'application/json',origin:env.VIEWER_ORIGIN},body:JSON.stringify(requestBody),...extra})}
describe('control plane app',()=>{it('requires bearer auth',async()=>{const app=createApplication(()=>createMockProvider());const request=jobRequest({headers:{'content-type':'application/json'}});expect((await app(request,env)).status).toBe(401)});it('rejects unexpected browser origins',async()=>{const app=createApplication(()=>createMockProvider());const request=jobRequest();request.headers.set('origin','https://evil.example');expect((await app(request,env)).status).toBe(403)});it('returns a canonical execution envelope',async()=>{const app=createApplication(()=>createMockProvider({stdout:'2\n'}));const response=await app(jobRequest(),env);expect(response.status).toBe(200);const value=await response.json();expect(value.status).toBe('completed');expect(value.execution.provider).toBe('mock');expect(value.result.stdout).toBe('2\n');expect(value.provenance.source_sha256).toMatch(/^[0-9a-f]{64}$/)});it('does not turn provider failure into a judgment state',async()=>{const app=createApplication(()=>({async executePython(){throw new Error('provider unavailable')}}));const response=await app(jobRequest(),env);const value=await response.json();expect(response.status).toBe(502);expect(value.error).toBe('provider_failure');expect(value).not.toHaveProperty('judgment')})})
